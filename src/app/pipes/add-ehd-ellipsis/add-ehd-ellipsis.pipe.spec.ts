import { AddEhdEllipsisPipe } from './add-ehd-ellipsis.pipe';

describe('AddEhdEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new AddEhdEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
